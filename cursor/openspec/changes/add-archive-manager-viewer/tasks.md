## 1. 工程脚手架

- [x] 1.1 创建 Android 工程（Kotlin），确定 `minSdk`/`targetSdk`，配置基础依赖（Gradle、AndroidX）
- [x] 1.2 引入图片加载库（Glide 或 Coil）并完成基础集成
- [x] 1.3 搭建基础导航与主题，确立"列表 → 浏览 → 看图"简易明了的页面骨架

## 2. ArchiveProvider 抽象层

- [x] 2.1 定义 `ArchiveProvider` 接口与 `ArchiveEntry` 数据模型（名称/大小/是否目录/类型/`supportsRandomAccess`）
- [x] 2.2 实现 Provider 工厂：按文件后缀/魔数选择对应 Provider
- [x] 2.3 实现 `ZipProvider`（commons-compress `ZipFile` + 文件描述符随机访问：`listEntries` 读中央目录、`openStream` 对单 entry 开流），声明支持随机访问
- [x] 2.4 实现不支持随机访问格式的降级缓存基类（后台解压到私有缓存、准备状态回调），并以 `TarProvider` 落地

## 3. 文件来源与存储访问

- [x] 3.1 接入系统文件选择器（SAF / `ACTION_OPEN_DOCUMENT`）选择压缩包并持久化可持久 Uri 权限
- [x] 3.2 接入"分享到本应用"的 Intent，接收外部分享的压缩包
- [x] 3.3 实现授权失效检测与"重新选择"引导

## 4. 压缩包管理（archive-management）

- [x] 4.1 实现压缩包列表存储与展示（名称、格式、大小）
- [x] 4.2 实现基础操作：打开、重命名、删除（含是否删除源文件的选择）
- [x] 4.3 实现"查看信息"（名称/格式/大小/条目数等元信息）

## 5. 包内浏览（archive-browsing）

- [x] 5.1 实现免解压条目列表 UI，支持进入子目录与返回上一级
- [x] 5.2 接入降级格式的"正在准备…"状态展示
- [x] 5.3 实现嵌套压缩包的递归打开与浏览

## 6. 图册查看器（image-viewing）

- [x] 6.1 将 `ArchiveProvider.openStream` 包装为图片库自定义数据源（Coil `Fetcher`）
- [x] 6.2 实现图册翻页（前/后一张）与相邻图片预加载（窗口限制前后各 1~2 张）
- [x] 6.3 实现缩放与平移手势
- [x] 6.4 实现解码降采样的大图 OOM 防护（交由 Coil 按显示尺寸降采样）

## 7. 打开方式路由（open-with）

- [x] 7.1 实现按文件类型路由到内置查看器（图片→图册、文本→预览）
- [x] 7.2 实现"用其他应用打开"的外部 Intent 转交（含内容提供与无可用应用的提示）

## 8. 收尾

- [ ] 8.1 缓存与内存压力测试（大包、多图、超大图、低存储空间清理）— 需真机/模拟器，待环境就绪
- [ ] 8.2 主路径回归验证：覆盖每条规范场景 — 需真机/模拟器，待环境就绪
- [x] 8.3 完善 README 与基础使用说明
